#include "Builder.h"

using namespace mlir;
namespace astl {

void AstlBuilder::buildAstlMoudle(const std::string& name, const std::string& path) {
  // auto mOp = builder.create<ModuleOp>(unknownLoc, name);
  // auto fOp = builder.create<FuncOp>(unknownLoc, "main", builder.getFunctionType({}, {}));
  // auto entryBlock = fOp.addEntryBlock();
  // builder.setInsertionPointToStart(entryBlock);
  // builder.create<mlir::ConstantOp>(unknownLoc, builder.getUnitAttr());
}



};